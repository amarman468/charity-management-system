// AdminSidebar removed — top navigation links are used instead.
// This module remains to avoid import errors if any leftover references exist.
const AdminSidebar = () => null;

export default AdminSidebar;
